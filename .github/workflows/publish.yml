name: Publish to PyPI and TestPyPI

on:
  push:
    tags:
      - 'v*.*.*'
  release:
    types: [published]
  workflow_dispatch:

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build sdist and wheel
        run: |
          python -m pip install --upgrade pip build twine
          python -m build
          python -m twine check dist/*

      - name: Publish to TestPyPI (RC)
        if: (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') && contains(github.ref, '-rc')) || (github.event_name == 'release' && contains(github.event.release.tag_name, '-rc'))
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

      - name: Publish to PyPI
        if: (github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') && !contains(github.ref, '-rc')) || (github.event_name == 'release' && !contains(github.event.release.tag_name, '-rc')) || (github.event_name == 'workflow_dispatch')
        uses: pypa/gh-action-pypi-publish@release/v1
